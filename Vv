getgenv().DIBUConfig = {

    -- [[ Main ]] --
    AutoFarm = true,
    JobMode  = "fishing",       -- fishing/stocker/cooking/atm_swiper/janitor/farming
    Farming  = false,           
    Hud      = true,            -- ✅ เปิดเมนู/แถบสถานะแล้ว

    -- [[ Job Randomizer ]] --
    JobRandomizer         = false,
    JobRandomizerInterval = 30, -- minutes
    JobRandomizerPool     = { "atm_swiper", "stocker", "cooking", "janitor", "fishing", "farming" },

    -- [[ Money ]] --
    AutoDeposit      = true,
    DepositThreshold = 200,
    DepositAll       = true,

    -- [[ Safety ]] --
    AntiKill     = true,
    AntiRagdoll  = true,
    AutoRespawn  = true,
    AutoHit      = true,
    StaminaFarm  = true,
    HideUsername = false,

    -- [[ Server Hopping ]] --
    MoneyStallRejoin    = true,
    MoneyStallMins      = 15,
    AutoHop             = true,
    AutoHopInterval     = 60,   -- minutes (0 = off)
    AutoHopMaxPlayers   = 0,    -- hop if server over this (0 = off)
    HopTargetMaxPlayers = 8,    -- only hop to servers at/under this
    AutoLowServerHop    = false,
    HopOnDeath          = true,

    -- [[ Vehicle ]] --
    VehicleType       = "car",  -- bike/car
    Vehicle           = "auto", -- auto / vehicle name
    SwiperUseVehicle  = true,
    AutoRepairVehicle = true,

    -- [[ ATM Swiper ]] --
    AutoBuyTools      = true,
    BulkBuyCount      = 5,
    SwiperUnderground = false,

    -- [[ Stocker ]] --
    BestBox = true,
    BoxType = "NormalBox",      -- NormalBox/SunflowerSeedsBox/ElectronicsBox/GoldenBox

    -- [[ Cook ]] --
    AutoBuySkillet = true,

    -- [[ Janitor ]] --
    PuddleTarget = "nearest",   -- smart/nearest

    -- [[ Farming ]] --
    FarmSeed           = "smart", -- smart/SunflowerSeeds/CornSeeds/TomatoSeeds/GoldenTomatoSeeds
    FarmWater          = true,
    FarmWaterThreshold = 60,      -- water below this % (10-95)
    FarmPotUpgrade     = true,

    -- [[ Fishing ]] --
    UndergroundMode   = "full", -- full/semi/off
    FishingUseVehicle = true,
    BaitPerTrip       = 12,
    FishSell          = 12,     -- sell every N fish
    FishSpot          = "smart", -- smart / spot number
    FishingRod        = "smart",
    FishingBait       = "smart",
    FishSkipSell      = false,
    FishSafeStore     = false,
    FishSafeTiers     = {},     -- Uncommon/Rare/Epic/Legendary/Omega
    FishSellNow       = true,   -- instant-sell a rare on catch
    FishSellNowTiers  = { "Legendary", "Omega" },

    FishSkipType        = "cancel", -- off/cancel/drop
    FishSkipRarities    = { "Common", "Uncommon", "Rare", "Epic" }, -- thrown back; empty = keep everything
    FishKeepOnlyLvl70   = true,     
    FishSpot70MinRarity = "Uncommon", 
    FishStaminaFarm     = false,   

    FishWebhook        = "",    -- your Discord webhook URL
    FishWebhookEnabled = true,
    FishNotifyTiers    = { "Legendary", "Omega" },

    -- [[ Display ]] --
    FpsBoost            = false,
    FpsBoostExtreme     = false,
    Whitescreen         = false,
    SkipSpinAnimation   = true,
}

-- ==============================================
-- 📋 ส่วนสร้างเมนู — เพิ่มตรงนี้เลย
-- ==============================================
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local PlayerGui = player:WaitForChild("PlayerGui")

-- หน้าจอเมนู
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DIBU_Menu"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = PlayerGui

-- กรอบหลัก
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 280, 0, 420)
MainFrame.Position = UDim2.new(0.02, 0, 0.5, -210)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.CornerRadius = UDim.new(0, 12)
MainFrame.Parent = ScreenGui

-- หัวเมนู
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
Title.Text = "DIBU Auto — เมนูควบคุม"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16
Title.Parent = MainFrame

-- ปุ่ม AutoFarm
local AutoFarmBtn = Instance.new("TextButton")
AutoFarmBtn.Size = UDim2.new(0.9, 0, 0, 45)
AutoFarmBtn.Position = UDim2.new(0.05, 0, 0, 65)
AutoFarmBtn.BackgroundColor3 = getgenv().DIBUConfig.AutoFarm 
    and Color3.fromRGB(46, 204, 113) 
    or Color3.fromRGB(231, 76, 60)
AutoFarmBtn.Text = "AutoFarm: "..tostring(getgenv().DIBUConfig.AutoFarm)
AutoFarmBtn.TextColor3 = Color3.new(1, 1, 1)
AutoFarmBtn.Font = Enum.Font.Gotham
AutoFarmBtn.TextSize = 14
AutoFarmBtn.Parent = MainFrame

AutoFarmBtn.MouseButton1Click:Connect(function()
    getgenv().DIBUConfig.AutoFarm = not getgenv().DIBUConfig.AutoFarm
    AutoFarmBtn.Text = "AutoFarm: "..tostring(getgenv().DIBUConfig.AutoFarm)
    AutoFarmBtn.BackgroundColor3 = getgenv().DIBUConfig.AutoFarm 
        and Color3.fromRGB(46, 204, 113) 
        or Color3.fromRGB(231, 76, 60)
end)

-- ปุ่ม AutoHop
local AutoHopBtn = Instance.new("TextButton")
AutoHopBtn.Size = UDim2.new(0.9, 0, 0, 45)
AutoHopBtn.Position = UDim2.new(0.05, 0, 0, 125)
AutoHopBtn.BackgroundColor3 = getgenv().DIBUConfig.AutoHop 
    and Color3.fromRGB(46, 204, 113) 
    or Color3.fromRGB(231, 76, 60)
AutoHopBtn.Text = "AutoHop: "..tostring(getgenv().DIBUConfig.AutoHop)
AutoHopBtn.TextColor3 = Color3.new(1, 1, 1)
AutoHopBtn.Font = Enum.Font.Gotham
AutoHopBtn.TextSize = 14
AutoHopBtn.Parent = MainFrame

AutoHopBtn.MouseButton1Click:Connect(function()
    getgenv().DIBUConfig.AutoHop = not getgenv().DIBUConfig.AutoHop
    AutoHopBtn.Text = "AutoHop: "..tostring(getgenv().DIBUConfig.AutoHop)
    AutoHopBtn.BackgroundColor3 = getgenv().DIBUConfig.AutoHop 
        and Color3.fromRGB(46, 204, 113) 
        or Color3.fromRGB(231, 76, 60)
end)

-- ปุ่มซ่อน/แสดงเมนู
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0.9, 0, 0, 45)
ToggleBtn.Position = UDim2.new(0.05, 0, 0, 350)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(52, 152, 219)
ToggleBtn.Text = "ซ่อน/แสดง เมนู"
ToggleBtn.TextColor3 = Color3.new(1, 1, 1)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 14
ToggleBtn.Parent = MainFrame

local isVisible = true
ToggleBtn.MouseButton1Click:Connect(function()
    isVisible = not isVisible
    MainFrame.Visible = isVisible
end)
